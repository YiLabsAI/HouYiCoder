# Normalize line endings to LF in the repo and the working tree on every
# platform. GitHub's Windows runner checks out with core.autocrlf=true,
# which converts LF to CRLF; pinning LF prevents a future test that asserts
# on fixture line content from mysteriously failing on Windows.
* text=auto eol=lf

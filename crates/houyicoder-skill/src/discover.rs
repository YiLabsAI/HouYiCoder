//! Skill discovery: scans skill directories across three paths with
//! precedence managed > project > user and trust gating.

// Placeholder — implementation in next commit.

pub fn discover_skills() -> Vec<super::definition::SkillDefinition> {
    Vec::new()
}

//! Progressive-disclosure helpers beyond the listing + body layers.
//! The resource layer surfaces the files a skill keeps alongside its body
//! so the model can address them by path without a discovery call.

pub mod resources;
